# Configuration for Koha backup scripts
# Copy this file to config.txt and adjust values as needed
# Lines starting with # are comments

# Remote server credentials
USERNAME=backup
PASSWORD=your_password_here
IP=10.10.10.10

# SSH host key fingerprint (leave blank to auto-discover on first run)
# Or get from server: ssh-keygen -l -f /etc/ssh/ssh_host_ed25519_key.pub
HOST_FINGERPRINT=

# Number of backup files to keep per instance (older files are deleted)
RETENTION_FILES=30
